<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <title>Project Detail</title>
</head>
<body>
    <h1><%= project.name %></h1>
    <h2>Issues</h2>
    <ul>
        <% project.issues.forEach(issue => { %>
            <li><%= issue.title %> - <%= issue.author %></li>
        <% }); %>
    </ul>
    <a href="/create-issue/<%= project.name %>">Create New Issue</a>
</body>
</html>
